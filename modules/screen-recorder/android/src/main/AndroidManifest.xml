<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <application>
    <!--
      MediaProjection on Android 10+ must run inside a foreground service
      of type mediaProjection; the persistent notification doubles as the
      transparent "recording in progress" indicator required by product
      privacy rules.
    -->
    <service
      android:name="com.twk.screenrecorder.ScreenRecordService"
      android:exported="false"
      android:foregroundServiceType="mediaProjection" />
  </application>
</manifest>
