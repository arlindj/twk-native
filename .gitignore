# Dependencies
node_modules/
server/node_modules/

# Expo legacy cache (project is bare RN — never commit)
.expo/

# Bundler output
dist/

# iOS (bare native — project is tracked, build artifacts are not)
ios/build/
ios/Pods/
ios/.xcode.env.local
ios/DerivedData/
*.xcuserdatad/

# Android (bare native — project is tracked, build artifacts are not)
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/release/
*.hprof
*.keystore
!android/app/debug.keystore

# Metro
.metro-health-check*

# Debug
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# Local env
.env*.local

# Dev server artifacts
server/uploads/

# Maestro test artifacts
.maestro/tests/

# Typescript
*.tsbuildinfo
keystore.properties
.env
